-- Пустая миграция. Номер занят, потому что файл под этим именем уже уехал на
-- стенд и уронил там API: миграции применяет фабрика соединения, и исключение
-- внутри неё превращает в 500 любой запрос, включая вход.
--
-- Выкладка на сервер идёт без удаления файлов (scp без --delete), поэтому
-- откат коммита сам по себе стенд не поднимает — упавший файл остаётся на
-- диске и падает снова. Единственный способ убрать его действием штатного
-- деплоя — перезаписать тем же именем безвредным содержимым.
--
-- Правка названия заказчика договора 305 вернётся отдельной миграцией, когда
-- будет понятна причина падения (локально на временной MySQL тот же файл
-- отрабатывал и был идемпотентным).
SET @erp_migration_025_noop = 1;
